<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="link.moely.mobile"
    android:versionCode="12"
    android:versionName="1.0.3" >

    <uses-sdk
        android:minSdkVersion="29"
        android:targetSdkVersion="36" />

    <!-- ===== 网络相关权限 (保持不变) ===== -->
    <!-- 互联网权限：应用访问网络所必需 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 访问网络状态权限：检查网络连接状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 访问 WiFi 状态权限：检测 WiFi 连接 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- ===== 存储相关权限 ===== -->
    <!-- 存储权限：对于 Android 12 (API 32) 及以下版本。API 33+ 使用 READ_MEDIA_* -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 媒体文件访问权限：对于 Android 13 (API 33) 及更高版本 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- 通知权限：用于 DownloadManager 显示下载通知 (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 管理外部存储权限：用于访问壁纸等系统资源（可选） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- ===== WebView 功能增强权限 (保持不变) ===== -->
    <!-- 相机权限：用于网页中的相机功能（如扫码、拍照上传） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 录音权限：用于网页中的语音功能 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 地理位置权限：用于网页中的定位功能 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 声明相机和麦克风功能为可选（不强制要求设备支持） -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <!-- ===== 其他权限 (保持不变) ===== -->
    <!-- 震动权限：用于网页反馈 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 唤醒锁权限：保持屏幕常亮（视频播放时） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <permission
        android:name="link.moely.mobile.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="link.moely.mobile.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:extractNativeLibs="false"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoelyApp"
        android:usesCleartextTraffic="true" >

        <!-- ===== 主活动 (保持不变) ===== -->
        <activity
            android:name="link.moely.mobile.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:exported="true" >

            <!-- 应用启动入口 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 处理网页链接（Deep Links） -->
            <intent-filter android:autoVerify="true" >
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="www.moely.link" />
                <data android:host="mobile.moely.link" />
            </intent-filter>

            <!-- 自定义 Scheme -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="moely" />
            </intent-filter>

            <!-- 处理文件打开（如下载的HTML文件） -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="text/html" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="application/xhtml+xml" />
            </intent-filter>
        </activity>

        <!-- ===== 其他活动和 FileProvider (保持不变) ===== -->
        <activity
            android:name="link.moely.mobile.DownloadsActivity"
            android:exported="false"
            android:label="@string/title_activity_downloads"
            android:parentActivityName="link.moely.mobile.MainActivity" />
        <activity
            android:name="link.moely.mobile.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:parentActivityName="link.moely.mobile.MainActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="link.moely.mobile.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true" >
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="link.moely.mobile.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>